
export default class ProjectModel {
    constructor(id, title, description, etiquetas, imageUrl, url) {
      this.id = id;
      this.title = title;
      this.description = description;
      this.etiquetas = etiquetas;
      this.imageUrl = imageUrl;
      this.url = url;
    }
  }

  