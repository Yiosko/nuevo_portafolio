
export default class ProjectModel {
    constructor(id, title, description, imageUrl, url) {
      this.id = id;
      this.title = title;
      this.description = description;
      this.imageUrl = imageUrl;
      this.url = url;
    }
  }

  